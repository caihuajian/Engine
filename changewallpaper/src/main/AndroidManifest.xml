<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cc.gaylen.changewallpaper"
    android:versionCode="1"
    android:versionName="1.0.0">

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="17" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme.Light">
        <activity
            android:name="com.cc.gaylen.changewallpaper.index.AppStart"
            android:label="@string/app_name"
            android:theme="@style/concealTitle">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.cc.gaylen.changewallpaper.index.MainActivity"
            android:logo="@drawable/title_icon"></activity>
    </application>

</manifest>